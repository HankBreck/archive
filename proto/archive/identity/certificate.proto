syntax = "proto3";
package archive.identity;

message Certificate {
    uint64 id = 1;
	uint64 issuer_id = 2;
	string salt = 3;
	string metadata_schema_uri = 4;
	map<string, string> hashes = 5;
}

message Hash {
    string key = 1;
    string value = 2;
}